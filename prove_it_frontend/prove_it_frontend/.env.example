# Base URL of the backend API. Baked in at build time by Vite — set this before
# running `npm run build` for anything other than local dev against 127.0.0.1:8000.
VITE_API_BASE_URL=https://pr-tracker-z2vu.onrender.com
